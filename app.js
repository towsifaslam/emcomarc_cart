function increment() {
  let count = document.getElementById("count");
  if (count.value >= 5) {
    count.value = 5;
    alert("Only five product allow");
  } else {
    count.value++;
  }
}
function decrement() {
  let count = document.getElementById("count");
  if (count.value <= 0) {
    count.value = 0;
    alert("are you pol naki");
  } else {
    count.value--;
  }
}

function crement() {
  let count = document.getElementById("count");
  if (count.value >= 5) {
    count.value = 5;
    alert("Only five product allow");
  } else {
    count.value++;
  }
}
